# Required
version: 2

# Set the OS, Python version, and other tools you might need
build:
  os: ubuntu-24.04
  tools:
    nodejs: "16"
  html:
    directory: docs

# Skip Sphinx build
sphinx:
  configuration: null